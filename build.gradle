allprojects {
    repositories {
        jcenter()
        maven {
            name = "asm"
            url = "http://oss.sonatype.org/content/groups/public/"
        }
    }
}

subprojects {
    apply plugin: "java"

    group = "asmp"
    version = "1.0-SNAPSHOT"
}